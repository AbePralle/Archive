package compiler;

import alphavm.BardOp;

public class BardQualifiers
{
  final static public int
    is_primitive     = (1<<0),
    is_built_in      = (1<<1),
    is_native        = (1<<2),
    is_singleton     = (1<<3);
}

